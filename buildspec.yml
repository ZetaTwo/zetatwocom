version: 0.1
   
phases:
  install:
    commands:
      - gem install bundler execjs therubyracer
      - bundle install
  build:
    commands:
      - bundle exec jekyll build
      - aws s3 sync _site/ s3://www.zeta-two.com
